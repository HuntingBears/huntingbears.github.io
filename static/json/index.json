---
layout: null
---
{% raw %}{{% endraw %}{% for post in site.posts %}"{{ post.article_id }}":{% raw %}{{% endraw %}"title":"{{ post.title | escape }}","description":"{{ post.description | escape }}","keywords":"{% for tag in post.tags %}{% capture tag_slug %}{{ tag | downcase | replace: 'ñ', 'n' | replace: 'á', 'a' | replace: 'é', 'e' | replace: 'í', 'i' | replace: 'ó', 'o' | replace: 'ú', 'u' | replace: ' ', '-' | replace: '.', ' ' | uri_escape }}{% endcapture %}{{ tag_slug }}, {% endfor %}{% for category in post.categories %}{% capture cat_slug %}{{ category | downcase | replace: 'ñ', 'n' | replace: 'á', 'a' | replace: 'é', 'e' | replace: 'í', 'i' | replace: 'ó', 'o' | replace: 'ú', 'u' | replace: ' ', '-' | replace: '.', ' ' | uri_escape }}{% endcapture %}{{ cat_slug }}{% unless forloop.last %}, {% endunless %}{% endfor %}","image":"{{ post.image | escape }}","url":"{{ site.url }}{{ post.url }}","date":"{{ post.date | date_to_xmlschema }}"{% raw %}}{% endraw %}{% unless forloop.last %},{% endunless %}{% endfor %}{% raw %}}{% endraw %}
